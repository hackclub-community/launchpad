# Paths

`/src` - where all (or most at least) of the code is

`/src/convex` - backend stuff touching the database + database schema

`/src/routes` - where all the pages are

`/src/lib` - functions/utils

`/static` - static assets
